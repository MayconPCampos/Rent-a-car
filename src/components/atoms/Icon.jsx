import styled from "styled-components";

const Icon = styled.div`
  svg {
    fill: #a0a0a0;
    cursor: pointer;
    transform: scale(2.5);
    margin: 20px;
  }

  svg:hover {
    fill: #191509;
  }
`;

export default Icon;
